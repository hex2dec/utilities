# Utilities

## Code Manager

Build code-manager

```bash
cd code-manager
go build
```

Usage code-manager

```bash
code-manager -h

Usage of code-manager:
  -h    print usage message(shorthand)
    -help
            print usage message
```
